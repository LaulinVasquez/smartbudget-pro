<% if (errors && errors.length > 0) { %>
<div class="validation-errors">
    <ul>
        <% errors.forEach(error => { %>
            <li><%= error.msg %></li>
        <% }) %>
    </ul>
</div>
<% } %>